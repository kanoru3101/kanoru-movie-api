import {GetSearch} from "@controllers/search/types";


export const getSearch = async ({
  type,
  search,
  language,
}: GetSearch) => {
  let request = null;
  if (type === 'all') {
    request = await
  }


}
